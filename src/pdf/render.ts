// Report PDF rendering (Puppeteer or PDFShift — CLAUDE.md §4). Renders the Markdown
// report from Call 4 to a branded PDF for email delivery (PRD §3.2 Step 5).

/** Renders the report Markdown to PDF bytes. (Param unused until the renderer is built.) */
export async function renderReportPdf(_reportMarkdown: string): Promise<Uint8Array> {
  throw new Error('NOT_IMPLEMENTED: PDF rendering')
}
